# CIC Project Lab v5 Single File

Upload only `index.html` to the root of your GitHub repository.

This version has all CSS, JavaScript, and scenario data embedded in one file, so GitHub Pages does not need separate `src/` or `data/` folders.
